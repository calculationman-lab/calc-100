# v28 変更点・検証結果

基準：v27 ZIP（SHA-256 `890081519ef208b88f48f673cc143ff084ef199a4583073b41636c58de973352`）。成果物：v28。検証日：2026-09-18。

## 変更範囲

- 追加：`memory-data.js`（固定150問・有理数比較・4択）、`memory-core.js`（出題・タイマー・記録・習得・報酬）、`memory-ui.js`（画面・操作・保存・同時学習制御）、`memory.css`（暗記画面だけのスタイル）。
- 既存ファイル変更：`index.html`（入口・画面切替・学習ロック・v28）、`sw.js`（v28・追加ファイル・バージョン別キャッシュ参照）、`README.md`。
- 資料追加：`QUESTIONS.md`、このファイル。
- 元52ファイル中49ファイルをバイト単位で保持。manifest・全画像・全MP3は無変更。既存のインラインCSSは改行形式を除き一致。
- 既存ゲームスクリプトはバージョンと画面切替・学習ロックの接続部分だけを変更。計算生成・採点・EXP式・装備・ボス・図鑑の処理はv27を維持。
- `calcRPG.memoryTraining` の追加と共通EXP加算以外に、本編の記録・進行を増やさない。
- 元ZIP、旧v16作業フォルダー、別アプリ、実ユーザーのセーブは変更していない。

## 検証

| 項目 | 結果・証拠 |
|---|---|
| 全150問 | 平方数30・小数/分数40・分数/百分率30・積20・時間30。75組、150固定ID、おすすめ70問。独立した期待値・有理数計算で照合 |
| 4択 | 全問4個・正解1個・同値重複なし。各問40通りのシャッフル確認。表記の長さだけで正解が目立たない誤答候補も確認 |
| ロジック検証 | 暗記170/170、既存回帰69/69、ボス14/14（合計253件） |
| 3秒境界 | 2,999msは回答、3,000ms以降は時間切れ。二重回答・古いトークン・停止・残り時間再開を確認。実ブラウザ内の時計制御でも両境界を確認 |
| 正誤表示 | 正解300ms、誤答/時間切れ2,000ms。その後の問題に新たに3,000ms。非表示時は背後で進まない |
| 出題・習得 | 苦手再出題に3問以上を挟む、逆方向の連続を避ける、苦手0/1件、異なる3完了セッション、誤答によるリセット、途中終了を確認 |
| EXP・保存 | 299＋20＝319、19正解＋1時間切れ＝19 EXP、0正解＝0 EXP、途中終了/カード閲覧＝0 EXP。旧値・未知フィールド・設定保持、各回答保存、再読込、完了の二重実行を確認 |
| 保存失敗 | 回答保存と完了保存それぞれで失敗を注入。成功扱いせず停止し、再試行で1回だけ保存・加算。壊れたJSONを上書きしない |
| 操作 | 実Chromeで20問通し、マウス・タップ相当・1〜4キー、連打・キー長押し、終了取消、明示再開、再挑戦・TOPを確認 |
| 複数タブ | 暗記中の別タブ暗記/通常開始を抑止。退出・タブ終了後は開始可能 |
| 本編ブラウザ回帰 | 通常100問（99点→130 EXP）、3回クリアでボス、誤答1問復習、ボス19/20失敗・20/20成功、装備1個、討伐記録、HP/HIT/突進、図鑑・履歴を確認。暗記記録は維持 |
| 画面 | PC 1440×1000、iPad相当1180×820・820×1180、スマートフォン相当390×844・360×640。4択2×2、タップ領域、画面内の配置を実測しスクリーンショット確認 |
| PWA | 隔離HTTP環境・旧セーブとv27キャッシュからv28へ更新。旧キャッシュ削除・v28画面・保存保持を確認。旧キャッシュ再作成時も旧HTMLを返さない |
| オフライン | 再読込、通常計算、暗記全8コース・カード・回答保存。キャッシュ55項目すべての応答（画像・MP3・問題データを含む）を取得 |
| 構文・参照 | 既存/追加JS構文、manifest、キャッシュ参照ファイル実在、バージョン一致。元ZIP・confirm.mp3の指定ハッシュ一致 |

詳細なテスト・ログ・スクリーンショットは開発用ソースの `calc-100-next-dev/evidence/` と `tests/` に収録。主要ブラウザ検証は11項目、境界7項目、PWA7項目、本編6項目。ZIP再展開の構成・起動・20問完了は `release-validation.json` に別記する。

## 検証中に修正した点

- v27のボタン共通スタイルが暗記ヘッダーへ及んでいたため、暗記領域の幅・余白だけを調整。
- 時間問題で正解だけ桁数が異なるケースに、同じ単位・近い値の誤答候補を追加。問題ID・正答・出題数は不変。
- 連続タップが次問でダブルクリック扱いになるケースを修正。同一問題の確定済み状態と入力トークンで重複を防ぎ、次問への正当な素早いタップを受け付ける。
- 更新検証の非同期待機を、Promise自体ではなく実際のキャッシュ内容・制御切替を確認する方式へ修正。初期のタイムアウトはPASS扱いしていない。
- Service Workerの検索先をv28キャッシュに限定し、更新後の旧HTML混入を防ぐ。キャッシュ削除は計算RPG用の名前に限定。

## 制約と非該当

- 公開・push・自動デプロイは実施していない。
- 実iPad/Safari実機、ホーム画面PWA実機、実際の音量・聞き取りやすさは未確認。画面とタップはChromeの相当サイズ・入力エミュレーション。
- 画面非表示のテストはブラウザイベントを模擬。実OSのアプリ切替操作の証明とは区別する。
- オフラインMP3取得と再生失敗時の継続は確認したが、音の主観評価は含まない。
- 同時学習保護にはWeb Locks対応環境が必要。対応しない環境は開始を止めて案内する。
- 元からの静的アプリのためビルド・TypeScript型検査は非該当。新フレームワーク、外部API・DB・CDN、分析タグ、画像生成は追加していない。
- 必須の自動検証・梱包検証に未解決のブロッカーはない。実機未確認範囲は上記のとおり。
